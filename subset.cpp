#include <iostream>
#include <vector>
#include <set>
#include <string>
using namespace std;
void subsets(vector<int>vec,int i,vector<int>stored){
    
}
    

int main(){
    vector<int>vec={1,2,3};
    vector<int>stored;
    subsets(vec,0,stored);
    return 0;
}